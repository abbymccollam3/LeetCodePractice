class Solution(object):
    def twoSum(self, nums, target):
        """
        :type nums: List[int]
        :type target: int
        :rtype: List[int]
        """
        nums.sort()
        ans = []
        n = len(nums)
        
        left = 0
        right = n - 1

        while left < right:
            

        for i in range(n):


        return ans
        

        # subtract nums[i] from target and see if we get the answer
        # for i in range(n):
        #     
        #     ans.append(i)
        #     for j in range(i+1, n):
        #         find = target - nums[i]
        #         print(find)
        #         if find == 0:
        #             ans.append(j)
        #             return ans
            
        # return ans